export type SVGProps = {
    size?: number | string; // Allow size to be set as number (px) or string (e.g., '1em')
    color?: string; // Allow color to be customizable
    className?: string; // Allow custom classes
};

